# Tack Branding

`logo/` is the canonical source for approved Tack identity assets. Runtime copies elsewhere in the repository exist because the website, extension, and desktop packaging require assets within their own bundles.

Historical concepts and visual references live under `documentation/brand-explorations/`; they are not approved production assets.
