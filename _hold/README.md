# Local Hold Area

Nothing in this directory is active product source.

- `design-explorations/`: unused mockups and screenshots with no source references.
- `legacy-branding/`: unrelated or retired illustration assets.
- `legacy-builds/`: superseded extension archives and an unidentified image archive.

The directory is ignored by Git so local historical material does not enter agency handoff commits. Review manually before deleting anything.
